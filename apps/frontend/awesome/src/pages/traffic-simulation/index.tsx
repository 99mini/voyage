import RootLayout from '@/components/layout/root-layout';

import TrafficSimulationSection from './components/traffic-simulation-section';

const TrafficSimulationPage = () => {
  return (
    <RootLayout>
      <TrafficSimulationSection />
    </RootLayout>
  );
};

export default TrafficSimulationPage;
